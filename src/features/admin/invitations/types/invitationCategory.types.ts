export type InvitationTemplateCategory = {
  id: string;
  slug: string;
  title: string;
  sortOrder: number;
  isActive: boolean;
  createdAt: string;
};

export type InvitationTemplateCategoryRow = {
  id: string;
  slug: string;
  title: string;
  sort_order: number;
  is_active: boolean;
  created_at: string;
};
